# Podcast Control Plane Plugin

Phase-1 scaffold for a plugin-first podcast workflow control plane.

This package intentionally ships only the minimum installable structure:

- plugin manifest
- worker health handler
- page, settings page, dashboard widget, and project detail tab UI slots

Later slices will add plugin state, workflow CRUD, curated actions, and project bindings.
